name: ephemeral_platforms
description: Re-creates the platform directories and applies modifications from your config.
version: 0.1.0
repository: https://github.com/alexeyinkin/dart-ephemeral-platforms

environment:
  sdk: ^3.11.1

dependencies:
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  process_run: ^1.3.2
  total_lints: ^3.11.0
  test: ^1.31.1
